{
  "version": 2,
  "projects": [
    {
      "name": "adconfirm-web",
      "rootDirectory": "apps/web",
      "framework": "nextjs",
      "buildCommand": "cd ../.. && pnpm turbo build --filter=@adconfirm/web",
      "installCommand": "pnpm install --frozen-lockfile"
    },
    {
      "name": "adconfirm-dashboard",
      "rootDirectory": "apps/dashboard",
      "framework": "nextjs",
      "buildCommand": "cd ../.. && pnpm turbo build --filter=@adconfirm/dashboard",
      "installCommand": "pnpm install --frozen-lockfile"
    }
  ]
}
